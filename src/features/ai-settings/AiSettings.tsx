import { useEffect,useState } from 'react'
import { supabase } from '../../lib/supabase'
import { Card, PageHeader } from '../../components/Ui'
import type { Organization,Profile } from '../../types/domain'
interface Settings{organization_id:string;knowledge_only:boolean;allow_general_knowledge:boolean;rag_top_k:number;min_similarity:number;recent_messages_count:number;summarize_after_count:number;max_output_tokens:number;human_handoff_threshold:number;daily_message_limit:number|null;monthly_message_limit:number|null}
export function AiSettings({profile}:{profile:Profile}){const [orgs,setOrgs]=useState<Organization[]>([]);const [orgId,setOrgId]=useState(profile.organization_id??'');const [s,setS]=useState<Settings|null>(null);const [msg,setMsg]=useState('');useEffect(()=>{void supabase.from('organizations').select('*').then(r=>setOrgs((r.data??[]) as Organization[]))},[]);useEffect(()=>{if(!orgId){setS(null);return}void supabase.from('organization_settings').select('*').eq('organization_id',orgId).maybeSingle().then(r=>setS(r.data as Settings|null))},[orgId]);const save=async()=>{if(!s)return;const {error}=await supabase.from('organization_settings').update(s).eq('organization_id',s.organization_id);setMsg(error?.message??'تم الحفظ.')};return <><PageHeader title="إعدادات AI"/>{profile.role==='SUPER_ADMIN'&&<Card><select value={orgId} onChange={e=>setOrgId(e.target.value)}><option value="">اختر الجهة</option>{orgs.map(o=><option key={o.id} value={o.id}>{o.name_ar}</option>)}</select></Card>}{s&&<Card><div className="settings-grid"><label><span>Knowledge only</span><input type="checkbox" checked={s.knowledge_only} onChange={e=>setS({...s,knowledge_only:e.target.checked})}/></label><label><span>Allow general knowledge</span><input type="checkbox" checked={s.allow_general_knowledge} onChange={e=>setS({...s,allow_general_knowledge:e.target.checked})}/></label><label>Top K<input type="number" value={s.rag_top_k} onChange={e=>setS({...s,rag_top_k:Number(e.target.value)})}/></label><label>Min similarity<input type="number" step="0.01" value={s.min_similarity} onChange={e=>setS({...s,min_similarity:Number(e.target.value)})}/></label><label>Recent messages<input type="number" value={s.recent_messages_count} onChange={e=>setS({...s,recent_messages_count:Number(e.target.value)})}/></label><label>Summary threshold<input type="number" value={s.summarize_after_count} onChange={e=>setS({...s,summarize_after_count:Number(e.target.value)})}/></label><label>Max output<input type="number" value={s.max_output_tokens} onChange={e=>setS({...s,max_output_tokens:Number(e.target.value)})}/></label><label>Handoff threshold<input type="number" step="0.01" value={s.human_handoff_threshold} onChange={e=>setS({...s,human_handoff_threshold:Number(e.target.value)})}/></label></div><button onClick={()=>void save()}>حفظ</button>{msg&&<p>{msg}</p>}</Card>}</>}
