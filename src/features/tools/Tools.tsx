import { useEffect,useState } from 'react'
import { adminApi } from '../../lib/adminApi'
import { resourceAdmin } from '../../lib/resourceAdmin'
import { supabase } from '../../lib/supabase'
import { Badge, Card, Empty, FieldHint, Modal, PageHeader, PanelHeader } from '../../components/Ui'
import { useI18n } from '../../lib/i18n'
import type { Organization } from '../../types/domain'

interface Tool{id:string;organization_id:string;name:string;code:string;method:'GET'|'POST';endpoint_url:string;auth_type:string|null;is_read_only:boolean;requires_verification:boolean;requires_human_approval:boolean;timeout_seconds:number;is_active:boolean}
type AuthType='none'|'bearer'|'api_key'

export function Tools(){
  const {tr,valueLabel}=useI18n();const [rows,setRows]=useState<Tool[]>([]);const [orgs,setOrgs]=useState<Organization[]>([]);const [msg,setMsg]=useState('')
  const [form,setForm]=useState({organization_id:'',name:'',code:'',method:'GET' as 'GET'|'POST',endpoint_url:'',auth_type:'none' as AuthType,credential:'',header:'X-API-Key',is_read_only:true,requires_verification:false,requires_human_approval:false})
  const [editing,setEditing]=useState<Tool|null>(null);const [editName,setEditName]=useState('');const [editMethod,setEditMethod]=useState<'GET'|'POST'>('GET');const [editUrl,setEditUrl]=useState('');const [editAuth,setEditAuth]=useState<AuthType>('none');const [editVerify,setEditVerify]=useState(false);const [editApproval,setEditApproval]=useState(false);const [editTimeout,setEditTimeout]=useState(10)
  const load=async()=>{const [toolsResult,organizationsResult]=await Promise.all([supabase.from('agent_tools_safe').select('*').order('created_at',{ascending:false}),supabase.from('organizations').select('*').eq('is_active',true).order('name_ar')]);setRows((toolsResult.data??[]) as Tool[]);const organizationRows=(organizationsResult.data??[]) as Organization[];setOrgs(organizationRows);setForm(current=>current.organization_id||organizationRows.length!==1?current:{...current,organization_id:organizationRows[0].id})}
  useEffect(()=>{void load()},[])
  const create=async(event:React.FormEvent)=>{event.preventDefault();setMsg('');if(!form.is_read_only){setMsg(tr('في النسخة الحالية يجب أن تكون الأداة للقراءة فقط.','In the current MVP, tools must be read-only.'));return}const toolSecret=form.auth_type==='bearer'?{token:form.credential}:form.auth_type==='api_key'?{header:form.header,value:form.credential}:undefined;try{await adminApi({action:'create_agent_tool',organizationId:form.organization_id,tool:{name:form.name,code:form.code,method:form.method,endpointUrl:form.endpoint_url,authType:form.auth_type,requestSchema:{},responseSchema:{},isReadOnly:true,requiresVerification:form.requires_verification,requiresHumanApproval:form.requires_human_approval,timeoutSeconds:10},toolSecret});setMsg(tr('تم إنشاء الأداة وحفظ بيانات الاعتماد في Vault.','Tool created and credentials stored in Vault.'));setForm(current=>({...current,name:'',code:'',endpoint_url:'',credential:''}));await load()}catch(error){setMsg(error instanceof Error?error.message:tr('تعذر إنشاء الأداة.','Unable to create tool.'))}}
  const openEdit=(row:Tool)=>{setEditing(row);setEditName(row.name);setEditMethod(row.method);setEditUrl(row.endpoint_url);setEditAuth((row.auth_type??'none') as AuthType);setEditVerify(row.requires_verification);setEditApproval(row.requires_human_approval);setEditTimeout(row.timeout_seconds)}
  const save=async(e:React.FormEvent)=>{e.preventDefault();if(!editing)return;try{await resourceAdmin({action:'update_tool',id:editing.id,name:editName,method:editMethod,endpointUrl:editUrl,authType:editAuth,requiresVerification:editVerify,requiresHumanApproval:editApproval,timeoutSeconds:editTimeout});setMsg(tr('تم حفظ تعديلات الأداة. بيانات الاعتماد لم تتغير.','Tool changes saved. Credentials were not changed.'));setEditing(null);await load()}catch(error){setMsg(error instanceof Error?error.message:tr('تعذر تعديل الأداة.','Unable to update tool.'))}}
  const toggle=async(row:Tool)=>{if(row.is_active&&!confirm(tr('تعطيل الأداة يمنع الوكيل من استخدامها. متابعة؟','Disabling the tool prevents the agent from using it. Continue?')))return;try{await resourceAdmin({action:'set_tool_active',id:row.id,isActive:!row.is_active});setMsg(tr(row.is_active?'تم تعطيل الأداة.':'تم تفعيل الأداة.',row.is_active?'Tool disabled.':'Tool enabled.'));await load()}catch(error){setMsg(error instanceof Error?error.message:tr('تعذر تغيير حالة الأداة.','Unable to change tool status.'))}}
  const remove=async(row:Tool)=>{if(!confirm(tr('حذف الأداة نهائيًا؟ إذا كان لها سجل تنفيذ فسيُرفض الحذف ويجب تعطيلها بدلًا من ذلك.','Permanently delete this tool? If it has execution history, deletion will be blocked and you should disable it instead.')))return;try{await resourceAdmin({action:'delete_tool',id:row.id});setMsg(tr('تم حذف الأداة.','Tool deleted.'));await load()}catch(error){setMsg(error instanceof Error?error.message:tr('تعذر حذف الأداة.','Unable to delete tool.'))}}

  return <div className="screen screen-tools">
    <PageHeader title={tr('أدوات الوكيل','Agent Tools')} description={tr('إنشاء وتعديل وتعطيل أو حذف أدوات القراءة المسبقة التعريف، مع بقاء الأسرار في Supabase Vault.','Create, edit, disable, or delete predefined read tools while keeping credentials in Supabase Vault.')}/>
    <Card className="form-panel tool-builder"><PanelHeader title={tr('تعريف أداة جديدة','Define a new tool')} description={tr('حدد نقطة الاتصال والمصادقة وسياسات الأمان.','Define endpoint, authentication, and safety policies.')} meta={<Badge tone="good">{tr('قراءة فقط','Read only')}</Badge>}/><form className="tool-form" onSubmit={create}><label>{tr('الجهة','Organization')}<select required value={form.organization_id} onChange={e=>setForm({...form,organization_id:e.target.value})}><option value="">{tr('اختر الجهة','Select organization')}</option>{orgs.map(org=><option key={org.id} value={org.id}>{org.name_ar} / {org.name_en??org.code}</option>)}</select></label><label>{tr('اسم الأداة','Tool name')}<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>{tr('الكود','Code')}<input required dir="ltr" value={form.code} onChange={e=>setForm({...form,code:e.target.value})} placeholder="GET_APPLICATION_STATUS"/></label><label>{tr('طريقة الطلب','Request method')}<select value={form.method} onChange={e=>setForm({...form,method:e.target.value as 'GET'|'POST'})}><option>GET</option><option>POST</option></select></label><label className="tool-endpoint-field">{tr('رابط نقطة الاتصال','Endpoint URL')}<input required type="url" dir="ltr" value={form.endpoint_url} onChange={e=>setForm({...form,endpoint_url:e.target.value})} placeholder="https://api.example.com/status"/></label><label>{tr('نوع المصادقة','Authentication type')}<select value={form.auth_type} onChange={e=>setForm({...form,auth_type:e.target.value as AuthType})}><option value="none">{tr('بدون مصادقة','No authentication')}</option><option value="bearer">Bearer</option><option value="api_key">API Key</option></select></label>{form.auth_type==='api_key'&&<label>{tr('اسم ترويسة المفتاح','API key header')}<input dir="ltr" value={form.header} onChange={e=>setForm({...form,header:e.target.value})}/></label>}{form.auth_type!=='none'&&<label className="tool-credential-field">{tr('بيانات الاعتماد','Credential')}<input required type="password" value={form.credential} onChange={e=>setForm({...form,credential:e.target.value})}/><FieldHint>{tr('تُحفظ في Vault ولا تظهر مرة أخرى.','Stored in Vault and not shown again.')}</FieldHint></label>}<fieldset className="tool-policy-strip"><legend>{tr('سياسات التنفيذ','Execution policies')}</legend><label className="check-label"><span><strong>{tr('قراءة فقط','Read only')}</strong></span><input type="checkbox" checked={form.is_read_only} onChange={e=>setForm({...form,is_read_only:e.target.checked})}/></label><label className="check-label"><span><strong>{tr('تحقق العميل','Customer verification')}</strong></span><input type="checkbox" checked={form.requires_verification} onChange={e=>setForm({...form,requires_verification:e.target.checked})}/></label><label className="check-label"><span><strong>{tr('موافقة بشرية','Human approval')}</strong></span><input type="checkbox" checked={form.requires_human_approval} onChange={e=>setForm({...form,requires_human_approval:e.target.checked})}/></label></fieldset><div className="form-submit-row"><button>{tr('إنشاء الأداة','Create tool')}</button></div></form>{msg&&<div className="inline-feedback" role="status">{msg}</div>}</Card>
    <Card className="table-card data-panel"><PanelHeader title={tr('الأدوات المسجلة','Registered tools')} description={tr('يمكن تعديل الإعدادات غير السرية أو تعطيل الأداة. الحذف يُمنع تلقائيًا إذا كان هناك سجل تنفيذ يجب الاحتفاظ به.','Edit non-secret settings or disable a tool. Deletion is automatically blocked when execution history must be preserved.')} meta={<Badge>{rows.length}</Badge>}/>{rows.length===0?<Empty>{tr('لا توجد أدوات.','No tools found.')}</Empty>:<table className="data-table"><thead><tr><th>{tr('الاسم','Name')}</th><th>{tr('الطريقة','Method')}</th><th>{tr('نقطة الاتصال','Endpoint')}</th><th>{tr('المصادقة','Authentication')}</th><th>{tr('الحالة','Status')}</th><th className="actions-cell">{tr('الإجراءات','Actions')}</th></tr></thead><tbody>{rows.map(row=><tr key={row.id} className={row.is_active?'':'soft-disabled'}><td className="cell-primary"><div>{row.name}</div><small>{row.code}</small></td><td><Badge>{row.method}</Badge></td><td><code>{row.endpoint_url}</code></td><td>{valueLabel(row.auth_type??'none')}</td><td><Badge tone={row.is_active?'good':'bad'}>{row.is_active?tr('نشطة','Active'):tr('متوقفة','Inactive')}</Badge></td><td className="actions-cell"><div className="row-actions"><button className="small ghost" onClick={()=>openEdit(row)}>{tr('تعديل','Edit')}</button><button className={`small ${row.is_active?'warning-action':'success-action'}`} onClick={()=>void toggle(row)}>{row.is_active?tr('تعطيل','Disable'):tr('تفعيل','Enable')}</button><button className="small danger-action" onClick={()=>void remove(row)}>{tr('حذف','Delete')}</button></div></td></tr>)}</tbody></table>}</Card>
    <Modal open={Boolean(editing)} onClose={()=>setEditing(null)} title={tr('تعديل أداة الوكيل','Edit agent tool')} description={tr('تعديل الإعدادات لا يعرض أو يغيّر السر المحفوظ في Vault.','Editing settings does not reveal or change the credential stored in Vault.')}><form className="modal-grid" onSubmit={save}><label>{tr('الاسم','Name')}<input required value={editName} onChange={e=>setEditName(e.target.value)}/></label><label>{tr('الطريقة','Method')}<select value={editMethod} onChange={e=>setEditMethod(e.target.value as 'GET'|'POST')}><option>GET</option><option>POST</option></select></label><label className="span-2">{tr('نقطة الاتصال','Endpoint URL')}<input required type="url" dir="ltr" value={editUrl} onChange={e=>setEditUrl(e.target.value)}/></label><label>{tr('المصادقة','Authentication')}<select value={editAuth} onChange={e=>setEditAuth(e.target.value as AuthType)}><option value="none">{tr('بدون','None')}</option><option value="bearer">Bearer</option><option value="api_key">API Key</option></select></label><label>{tr('المهلة بالثواني','Timeout seconds')}<input type="number" min={1} max={30} value={editTimeout} onChange={e=>setEditTimeout(Number(e.target.value))}/></label><label className="check-label"><span>{tr('يتطلب تحقق العميل','Requires verification')}</span><input type="checkbox" checked={editVerify} onChange={e=>setEditVerify(e.target.checked)}/></label><label className="check-label"><span>{tr('يتطلب موافقة بشرية','Requires human approval')}</span><input type="checkbox" checked={editApproval} onChange={e=>setEditApproval(e.target.checked)}/></label><div className="form-actions span-2"><button>{tr('حفظ التعديلات','Save changes')}</button><button type="button" className="ghost" onClick={()=>setEditing(null)}>{tr('إلغاء','Cancel')}</button></div></form></Modal>
  </div>
}
