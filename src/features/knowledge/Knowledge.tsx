import { useEffect,useState } from 'react'
import { supabase } from '../../lib/supabase'
import { Card, Empty, PageHeader } from '../../components/Ui'
import type { KnowledgeBase,KnowledgeDocument,Organization,Profile } from '../../types/domain'
import { useI18n, type AppLanguage } from '../../lib/i18n'

export function Knowledge({profile}:{profile:Profile}){const {language,tr,valueLabel}=useI18n();const [orgs,setOrgs]=useState<Organization[]>([]);const [organizationId,setOrganizationId]=useState(profile.organization_id??'');const [bases,setBases]=useState<KnowledgeBase[]>([]);const [docs,setDocs]=useState<KnowledgeDocument[]>([]);const [kbName,setKbName]=useState('');const [kbCode,setKbCode]=useState('');const [selectedKb,setSelectedKb]=useState('');const [contentLanguage,setContentLanguage]=useState<AppLanguage>(language);const [faqQ,setFaqQ]=useState('');const [faqA,setFaqA]=useState('');const [manualTitle,setManualTitle]=useState('');const [manualText,setManualText]=useState('');const [message,setMessage]=useState('')
 const load=async()=>{const [o,b,d]=await Promise.all([supabase.from('organizations').select('*'),supabase.from('knowledge_bases').select('*').order('created_at',{ascending:false}),supabase.from('knowledge_documents').select('*').order('created_at',{ascending:false})]);setOrgs((o.data??[]) as Organization[]);setBases((b.data??[]) as KnowledgeBase[]);setDocs((d.data??[]) as KnowledgeDocument[])};useEffect(()=>{void load()},[])
 const createKb=async(e:React.FormEvent)=>{e.preventDefault();const {data,error}=await supabase.from('knowledge_bases').insert({organization_id:organizationId,name:kbName,code:kbCode.toUpperCase().replace(/[^A-Z0-9_-]/g,'_')}).select('id').single();setMessage(error?.message??tr('تم إنشاء قاعدة المعرفة.','Knowledge base created.'));if(data){setKbName('');setKbCode('');setSelectedKb(data.id);await load()}}
 const addFaq=async(e:React.FormEvent)=>{e.preventDefault();const kb=bases.find(x=>x.id===selectedKb);if(!kb)return;const {error}=await supabase.from('knowledge_faq').insert({organization_id:kb.organization_id,knowledge_base_id:kb.id,question:faqQ,answer:faqA,language:contentLanguage});setMessage(error?.message??tr('تمت إضافة السؤال الشائع.','FAQ added.'));if(!error){setFaqQ('');setFaqA('')}}
 const addManual=async(e:React.FormEvent)=>{e.preventDefault();const kb=bases.find(x=>x.id===selectedKb);if(!kb)return;const {error}=await supabase.from('knowledge_documents').insert({organization_id:kb.organization_id,knowledge_base_id:kb.id,title:manualTitle,source_type:'manual_text',language:contentLanguage,processing_status:'pending',metadata:{manualText}});setMessage(error?.message??tr('تمت إضافة النص إلى قائمة المعالجة.','Manual text queued for processing.'));if(!error){setManualTitle('');setManualText('');await load()}}
 const upload=async(file:File)=>{const kb=bases.find(x=>x.id===selectedKb);if(!kb)return;const ext=file.name.split('.').pop()?.toLowerCase();if(!ext||!['pdf','docx','txt'].includes(ext)){setMessage(tr('المسموح PDF / DOCX / TXT فقط.','Only PDF / DOCX / TXT files are allowed.'));return}if(file.size>20*1024*1024){setMessage(tr('الحد الأقصى 20MB.','Maximum file size is 20MB.'));return}const id=crypto.randomUUID();const path=`${kb.organization_id}/${kb.id}/${id}/${file.name.replace(/[^\p{L}\p{N}._-]/gu,'_')}`;const {error:storageError}=await supabase.storage.from('knowledge').upload(path,file,{upsert:false});if(storageError){setMessage(storageError.message);return}const {error}=await supabase.from('knowledge_documents').insert({id,organization_id:kb.organization_id,knowledge_base_id:kb.id,title:file.name,source_type:'file',original_file_name:file.name,storage_path:path,language:contentLanguage,processing_status:'pending'});if(error){setMessage(error.message);return}setMessage(tr('تم الرفع ووضع المستند في قائمة المعالجة.','File uploaded and queued for processing.'));await load()}
 const visibleBases=organizationId?bases.filter(b=>b.organization_id===organizationId):bases;const visibleDocs=selectedKb?docs.filter(d=>d.knowledge_base_id===selectedKb):docs
 return <><PageHeader title={tr('المعرفة','Knowledge')} description={tr('قواعد المعرفة والمستندات والأسئلة الشائعة والنصوص اليدوية بالعربية والإنجليزية.','Knowledge bases, documents, FAQs, and manual text in Arabic and English.')}/><Card><div className="grid-form">{profile.role==='SUPER_ADMIN'&&<select value={organizationId} onChange={e=>{setOrganizationId(e.target.value);setSelectedKb('')}}><option value="">{tr('اختر الجهة','Select organization')}</option>{orgs.map(o=><option key={o.id} value={o.id}>{o.name_ar} / {o.name_en}</option>)}</select>}<select value={selectedKb} onChange={e=>setSelectedKb(e.target.value)}><option value="">{tr('اختر قاعدة المعرفة','Select knowledge base')}</option>{visibleBases.map(b=><option key={b.id} value={b.id}>{b.name}</option>)}</select><select value={contentLanguage} onChange={e=>setContentLanguage(e.target.value as AppLanguage)}><option value="ar">{tr('محتوى عربي','Arabic content')}</option><option value="en">{tr('محتوى إنجليزي','English content')}</option></select></div>{message&&<p>{message}</p>}</Card><div className="two-col"><Card><h2>{tr('إنشاء قاعدة معرفة','Create knowledge base')}</h2><form className="stack" onSubmit={createKb}><input required placeholder={tr('الاسم','Name')} value={kbName} onChange={e=>setKbName(e.target.value)}/><input required placeholder="CODE" value={kbCode} onChange={e=>setKbCode(e.target.value)}/><button disabled={!organizationId}>{tr('إنشاء','Create')}</button></form></Card><Card><h2>{tr('إضافة مصدر','Add source')}</h2><div className="stack"><input type="file" accept=".pdf,.docx,.txt" disabled={!selectedKb} onChange={e=>{const f=e.target.files?.[0];if(f)void upload(f)}}/><form className="stack" onSubmit={addManual}><input required placeholder={tr('عنوان النص اليدوي','Manual text title')} value={manualTitle} onChange={e=>setManualTitle(e.target.value)}/><textarea required rows={4} value={manualText} onChange={e=>setManualText(e.target.value)} placeholder={tr('النص','Text')}/><button disabled={!selectedKb}>{tr('إضافة نص يدوي','Add manual text')}</button></form></div></Card></div><Card><h2>{tr('الأسئلة الشائعة','FAQ')}</h2><form className="grid-form" onSubmit={addFaq}><input required placeholder={tr('السؤال','Question')} value={faqQ} onChange={e=>setFaqQ(e.target.value)}/><input required placeholder={tr('الإجابة','Answer')} value={faqA} onChange={e=>setFaqA(e.target.value)}/><button disabled={!selectedKb}>{tr('إضافة','Add')}</button></form></Card><Card><h2>{tr('المستندات','Documents')}</h2>{visibleDocs.length===0?<Empty>{tr('لا توجد مستندات.','No documents found.')}</Empty>:<table><thead><tr><th>{tr('العنوان','Title')}</th><th>{tr('النوع','Type')}</th><th>{tr('اللغة','Language')}</th><th>{tr('الحالة','Status')}</th><th>{tr('المعالجة','Processing')}</th></tr></thead><tbody>{visibleDocs.map(d=><tr key={d.id}><td>{d.title}</td><td>{valueLabel(d.source_type)}</td><td>{valueLabel(d.language)}</td><td>{d.is_active?tr('نشط','Active'):tr('متوقف','Inactive')}</td><td>{valueLabel(d.processing_status)}{d.processing_error&&<small className="error-text"> — {d.processing_error}</small>}</td></tr>)}</tbody></table>}</Card></>}
